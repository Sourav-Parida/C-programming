//Write a program to calculate the volume of a Cuboid.
#include<conio.h>
#include<stdio.h>

int main() {
    float len;
    printf("Enter the lenght of cuboid:---");
    scanf("%f",&len);
    printf("%f",len*len*len);
    getch();
    return 0;
}